# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        invisible: true

    created_at:
        label: awme.stocket::lang.tills.created_at
        type: datetime
        format:  D d/m/Y H:i
        searchable: true

    action:
        label: awme.stocket::lang.tills.action
        searchable: true

    seller:
        label: awme.stocket::lang.sales.customer
        searchable: true

    description:
        label: awme.stocket::lang.tills.description
        searchable: true

    cash:
        label: awme.stocket::lang.sales.cash

    credit_card: 
        label: awme.stocket::lang.sales.card

    total:
        label: awme.stocket::lang.sales.total

    till:
        label: awme.stocket::lang.tills.in_till